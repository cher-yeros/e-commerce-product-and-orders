#import './user.schema.graphql'
#import './product.schema.graphql'

type Order {
  id: ID!
  quantity: Int!
  totalPrice: Float!
  userId: ID
  createdAt: Date!
  updatedAt: Date!
  user: User
  products: [Product]
}