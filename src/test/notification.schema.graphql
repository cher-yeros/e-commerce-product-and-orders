#import './user.schema.graphql'
#import './order.schema.graphql'

type Notification {
  id: ID!
  message: String!
  isRead: Boolean!
  createdAt: Date!
  updatedAt: Date!
  userId: ID
  orderId: ID
  user: User
  order: Order
}